import { Meta, Story, Canvas, Props } from "@storybook/addon-docs/blocks";
import { ArgsTable, Stories } from "@storybook/addon-docs";
import DragDropFiles from ".";
import DragAndDropFile from "../DragAndDropFile";
import UploadedFile from "../../General/UploadedFile";

<Meta title="Inputs/Drag & Drop Files" component={DragDropFiles} />

export const TemplateDefault = (args) => <DragDropFiles {...args} />;

export const TemplateDragAndDropFile = (args) => <DragAndDropFile {...args} />;

export const TemplateSizes = (args) => <div>
    <DragDropFiles {...args} size="small"/>
    <DragDropFiles {...args} size="medium"/>
    <DragDropFiles {...args} size="large"/>
</div>;


export const TemplateColors = (args) => <div>
    <DragDropFiles {...args} color="primary"/>
    <DragDropFiles {...args} color="secondary"/>
    <DragDropFiles {...args} color="success"/>
    <DragDropFiles {...args} color="warning"/>
    <DragDropFiles {...args} color="danger"/>
</div>;


# Drag & Drop Files
```jsx
import { DragAndDropFiles } from "@lnc-react-ui";
```

<Canvas withSource="open">
  <Story name="Default">{TemplateDefault.bind({})}</Story>
</Canvas>

<ArgsTable story="Default" />

---

## [react-dropzone](https://react-dropzone.js.org/) is used in background.


## Subcomponents

<ArgsTable components={{ DragAndDropFile, UploadedFile }} />

---

## Accept Multiple Images

<Canvas withSource="open">
  <Story
    name="Accept Images"
    args={{
      multiple: true,
      accept: { "image/jpeg": [], "image/jpg": [], "image/png": [".png"] },
    }}
  >
    {TemplateDefault.bind({})}
  </Story>
</Canvas>

## With Files
  Show file size with `showFileSize={true}`. File size is provided in Bytes and it will formatted accordingly. 

<Canvas withSource="open">
  <Story
    name="With Files"
    args={{
      showFileSize: true,
      files: [
        { name: "File 1", size: 20000000 },
        { name: "File 2", size: 1500 },
      ],
    }}
  >
    {TemplateDefault.bind({})}
  </Story>
</Canvas>

## Drag & Drop File

`DragAndDropFile` component does not display file list, giving ability for custom file list display.

<Canvas withSource="open">
  <Story name="Drag and Drop File">{TemplateDragAndDropFile.bind({})}</Story>
</Canvas>

<ArgsTable story="Drag and Drop File" />

## Disabled With Alternative Labels

<Canvas>
  <Story
    name="Disabled With Alternative Labels"
    args={{
      disabled: true,
      selectFileText: "FILE",
      dndFileText: "This is the place where you should drag and drop your file",
    }}
  >
    {TemplateDefault.bind({})}
  </Story>
</Canvas>

## Sizes

<Canvas>
  <Story
    name="Sizes"
  >
    {TemplateSizes.bind({})}
  </Story>
</Canvas>

## Colors

<Canvas>
  <Story
    name="Colors"
  >
    {TemplateColors.bind({})}
  </Story>
</Canvas>