import { Meta, Story, Canvas, Props } from "@storybook/addon-docs/blocks";
import { ArgsTable, Stories } from "@storybook/addon-docs";
import { NotificationContainer, notification } from "./index";
import { useState } from "react";
import NotificationMessage from "./NotificationMessage";
import logo from '../../_utils/images/Image.png';
import Button from "../../General/Button";

<Meta title="Feedback/Notification" component={NotificationContainer} />

export const TemplateDefault = (args) => {
  const notify = () => notification("Message");
  return (
    <div>
      <Button onClick={notify} text="Notify!">Notify!</Button>
      <NotificationContainer {...args}/>
    </div>
  );
};

export const TemplateCustom= (args) => {
  const notify = () => notification(<NotificationMessage icon="user" title={"Title"}>Message</NotificationMessage>);
  return (
    <div>
      <Button onClick={() => notification(<NotificationMessage icon="user" title={"Title"}>Message</NotificationMessage>)} text="With Icon">With Icon</Button>
      <br /><br />
      <Button onClick={() => notification(<NotificationMessage avatar={true} avatarProps={{ image: logo }} title={"Title"} actions={<><Button text="Confirm">Confirm</Button><Button text="Dismiss">Dismiss</Button></>}>Message</NotificationMessage>)} text="With Action">With Action</Button>
      <br /><br />
      <Button onClick={() => notification(<NotificationMessage avatar={true} inlineActions={true} avatarProps={{ image: logo }} title={"Title"} actions={<a>Dismiss</a>}>Message</NotificationMessage>)} text="With Inline Actions">With Inline Actions</Button>
      <br /><br />
      <Button onClick={() => notification(<NotificationMessage sideButtons={true} avatar={true} avatarProps={{ image: logo }}  title={"Title"} actions={<><Button type="outline" color="secondary" text="Confirm" borderRadius="none">Confirm</Button><Button text="Dismiss" type="outline" color="neutral" borderRadius="none">Dismiss</Button></>}>Message</NotificationMessage>, {closeButton: false})} text="With Side Buttons">With Side Buttons</Button>
      <NotificationContainer {...args}/>
    </div>
  );
};

# Notification

```jsx
import {
  NotificationContainer,
  notification,
  NotifcationMessage,
} from "@lnc-react-ui";
```

<Canvas>
  <Story name="Default">{TemplateDefault.bind({})}</Story>
</Canvas>

<ArgsTable story="Default" />

### NotificationMessage Props

<ArgsTable components={{ NotificationMessage }} />

---

## Custom

<Canvas>
  <Story name="Custom">{TemplateCustom.bind({})}</Story>
</Canvas>

This component is build on [React-Toastify](https://www.npmjs.com/package/react-toastify). React-Toastify [documentaion](https://fkhadra.github.io/react-toastify/use-a-custom-close-button-or-remove-it/).