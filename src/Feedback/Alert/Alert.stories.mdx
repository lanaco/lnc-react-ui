import { Meta, Story, Canvas, Props } from "@storybook/addon-docs/blocks";
import { ArgsTable, Stories } from "@storybook/addon-docs";
import Alert from "./index";
import { useState } from "react";
import Button from "../../General/Button";
import Icon from "../../General/Icon";

<Meta title="Feedback/Alert" component={Alert} />

export const TemplateDefault = (args) => (
  <Alert {...args}
  ></Alert>
);

export const TemplateContentActions = (args) => (
  <Alert
    {...args}
    color={"danger"}
    title="No internet connection"
    actions={<a onClick={() => alert("Connection failed")}>Reconnect</a>}
  >
    Lorem ipsum dolor sit amet, consectetur adipiscing elit, sed do eiusmod
    tempor incididunt ut labore et sed.
  </Alert>
);

export const TemplateStatuses = (args) => (
  <div>
    <Alert
      {...args}
      color={"information"}
      title="A new software update is available. See what’s new in version 3.0.0."
    >
      Lorem ipsum dolor sit amet, consectetur adipiscing elit, sed do eiusmod
      tempor incididunt ut labore et sed.
    </Alert>
    <br />
    <Alert {...args} color={"success"} title="Order completed">
      Lorem ipsum dolor sit amet, consectetur adipiscing elit, sed do eiusmod
      tempor incididunt ut labore et sed.
    </Alert>
    <br />
    <Alert {...args} color={"warning"} title="Attention needed">
      Lorem ipsum dolor sit amet, consectetur adipiscing elit, sed do eiusmod
      tempor incididunt ut labore et sed.
    </Alert>
    <br />
    <Alert {...args} color={"danger"} title="No internet connection">
      Lorem ipsum dolor sit amet, consectetur adipiscing elit, sed do eiusmod
      tempor incididunt ut labore et sed.
    </Alert>
  </div>
);

export const TemplateCustom = (args) => {
  const [open, setOpen] = useState(true);
  return (
    <>
      {open && <Alert
        {...args}
        open={open}
        color={"secondary"}
        statusIcon={"user-circle"}
        title="Custom alert"
        actions={<><a onClick={() => alert("Connection failed")}>Connect</a><a onClick={() => setOpen(false)}>Dismiss</a></>}
      >
        Lorem ipsum dolor sit amet, consectetur adipiscing elit, sed do eiusmod
        tempor incididunt ut labore et sed.
      </Alert>}
      <br/>
      <Button disabled={open} onClick={() => setOpen(!open)} text="Open">Open</Button>
    </>
  );
};

export const TemplateCustomIcon = (args) => {
  const [open, setOpen] = useState(true);
  return (
    <>
      {open && <Alert
        {...args}
        open={open}
        color={"neutral"}
        statusIcon={<Icon icon="file"/>}
        title="Custom alert"
        actions={<><a onClick={() => alert("Connection failed")}>Connect</a><a onClick={() => setOpen(false)}>Dismiss</a></>}
      >
        Lorem ipsum dolor sit amet, consectetur adipiscing elit, sed do eiusmod
        tempor incididunt ut labore et sed.
      </Alert>}
      <br/>
      <Button disabled={open} onClick={() => setOpen(!open)} text="Open">Open</Button>
    </>
  );
};

# Alert

```jsx
import { Alert } from "@lnc-react-ui";
```

<Canvas>
  <Story name="Default" args={{ color: "information", title: "A new software update is available. See what’s new in version 3.0.0."}}>{TemplateDefault.bind({})}</Story>
</Canvas>

<ArgsTable story="Default" />

## Actions

<Canvas>
  <Story name="Actions">{TemplateContentActions.bind({})}</Story>
</Canvas>

## Statuses

<Canvas>
  <Story name="Statuses">{TemplateStatuses.bind({})}</Story>
</Canvas>

## Custom Alert

Default statusIcon is shown with `statusIcon={true}`, or not shown with `statusIcon={false}`. Custom status icon can be defined by setting `statusIcon` as `String`, or as element.

<Canvas withSource="open">
  <Story name="Custom">{TemplateCustom.bind({})}</Story>
</Canvas>


<Canvas>
  <Story name="Custom Element as Status Icon">{TemplateCustomIcon.bind({})}</Story>
</Canvas>