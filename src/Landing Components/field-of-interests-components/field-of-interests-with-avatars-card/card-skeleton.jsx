import { forwardRef } from "react";

import { SkeletonWrapper } from "./style";

const FieldOfInterestsWithAvatarsCardSkeleton = forwardRef(({}, ref) => {
  return <SkeletonWrapper></SkeletonWrapper>;
});

export default FieldOfInterestsWithAvatarsCardSkeleton;
