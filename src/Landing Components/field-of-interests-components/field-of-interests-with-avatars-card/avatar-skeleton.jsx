import { forwardRef } from "react";

import { AvatarSkeletonWrapper } from "./style";

const FieldOfInterestsWithAvatarsCardAvatarSkeleton = forwardRef(({}, ref) => {
  return <AvatarSkeletonWrapper></AvatarSkeletonWrapper>;
});

export default FieldOfInterestsWithAvatarsCardAvatarSkeleton;
