import { Meta, Story, Canvas, Props } from "@storybook/addon-docs/blocks";
import { ArgsTable, Stories } from "@storybook/addon-docs";
import PasswordInput from "./index";
import Icon from "../../General/Icon/index";

<Meta title="Basic Inputs/Password Input" component={PasswordInput} />

export const TemplateDefault = (args) => <PasswordInput {...args} />;

export const TemplateSizes = (args) => (
  <div style={{ display: "flex", flexDirection: "column", gap: "12px" }}>
    <PasswordInput {...args} size="small" placeholder="This is small input." />
    <PasswordInput
      {...args}
      size="medium"
      placeholder="This is medium input."
    />
    <PasswordInput {...args} size="large" placeholder="This is large input." />
  </div>
);

export const TemplateColors = (args) => (
  <div style={{ display: "flex", flexDirection: "column", gap: "12px" }}>
    <PasswordInput
      {...args}
      color="primary"
      placeholder="This is primary input."
    />
    <PasswordInput
      {...args}
      color="secondary"
      placeholder="This is secondary input."
    />
    <PasswordInput
      {...args}
      color="success"
      placeholder="This is success input."
    />
    <PasswordInput
      {...args}
      color="warning"
      placeholder="This is warning input."
    />
    <PasswordInput
      {...args}
      color="danger"
      placeholder="This is danger input."
    />
    <PasswordInput {...args} color="information" placeholder="This is info input." />
  </div>
);

export const TemplateAddons = (args) => (
  <div style={{ display: "flex", flexDirection: "column", gap: "12px" }}>
    <PasswordInput
      {...args}
      prefix={<Icon icon="ban" />}
      suffix="PASS"
      placeholder="Icon prefix and text suffix."
    />
  </div>
);

# Password Input

```jsx
import { PasswordInput } from "@lanaco/lnc-react-ui";
```

<Canvas>
  <Story name="Default" args={{}}>
    {TemplateDefault.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Default" />

## Read Only

<Canvas>
  <Story
    name="Read Only"
    args={{
      readOnly: true,
      value: "password",
    }}
  >
    {TemplateDefault.bind({})}
  </Story>
</Canvas>

## Disabled

<Canvas>
  <Story
    name="Disabled"
    args={{
      disabled: true,
      value: "password",
    }}
  >
    {TemplateDefault.bind({})}
  </Story>
</Canvas>

## Sizes

Defining `size` with `small`, `medium` and `large` values.

<Canvas>
  <Story name="Sizes" args={{}}>
    {TemplateSizes.bind({})}
  </Story>
</Canvas>

## Colors

<Canvas>
  <Story name="Colors" args={{}}>
    {TemplateColors.bind({})}
  </Story>
</Canvas>

## Prefix and suffix

<Canvas>
  <Story name="Prefix and suffix" args={{}}>
    {TemplateAddons.bind({})}
  </Story>
</Canvas>
