import { Meta, Story, Canvas, Props } from "@storybook/addon-docs/blocks";
import { ArgsTable, Stories } from "@storybook/addon-docs";
import PasswordInput from "./index";

<Meta title="Basic Inputs/Password Input" component={PasswordInput} />

export const TemplateDefault = (args) => <PasswordInput {...args} />;

export const TemplateSizes = (args) => (
  <div style={{ display: "flex", flexDirection: "column", gap: "12px" }}>
    <PasswordInput {...args} size="small" />
    <PasswordInput {...args} size="medium" />
    <PasswordInput {...args} size="large" />
  </div>
);

export const TemplateColors = (args) => (
  <div style={{ display: "flex", flexDirection: "column", gap: "12px" }}>
    <PasswordInput {...args} color="primary" />
    <PasswordInput {...args} color="secondary" />
    <PasswordInput {...args} color="success" />
    <PasswordInput {...args} color="warning" />
    <PasswordInput {...args} color="danger" />
    <PasswordInput {...args} color="info" />
  </div>
);

# Password Input
```jsx
import { PasswordInput } from "@lnc-react-ui";
```

<Canvas withSource="open">
  <Story name="Default" args={{}}>
    {TemplateDefault.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Default" />

## Read Only

<Canvas>
  <Story
    name="Read Only"
    args={{
      readOnly: true,
      placeholder: "Type text here",
      style: { width: "300px" },
    }}
  >
    {TemplateDefault.bind({})}
  </Story>
</Canvas>

## Disabled

<Canvas withSource="open">
  <Story
    name="Disabled"
    args={{
      disabled: true,
      placeholder: "Disabled",
      style: { width: "300px" },
    }}
  >
    {TemplateDefault.bind({})}
  </Story>
</Canvas>

## Sizes

Defining `size` with `small`, `medium` and `large` values.

<Canvas>
  <Story name="Sizes" args={{}}>
    {TemplateSizes.bind({})}
  </Story>
</Canvas>

## Colors

Defining `color`

<Canvas>
  <Story name="Colors" args={{}}>
    {TemplateColors.bind({})}
  </Story>
</Canvas>
