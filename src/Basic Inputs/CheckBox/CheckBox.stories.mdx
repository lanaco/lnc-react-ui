import { Meta, Story, Canvas, Props } from "@storybook/addon-docs/blocks";
import { ArgsTable, Stories } from '@storybook/addon-docs';
import CheckBox from "./index";
import Icon from "../../General/Icon";


<Meta title="Basic Inputs/CheckBox" component={CheckBox} />

export const TemplateDefault = (args) => (
    <CheckBox {...args} />
);

export const TemplateIcon = (args) => (
  <div style ={{display: 'flex', gap: '12px'}}>
    <CheckBox icon="heart" iconUncheckedStyle="regular" />
    <CheckBox checked icon="heart" iconUncheckedStyle="regular" />
    <CheckBox icon="shopping-cart" iconUncheckedStyle="solid" />
    <CheckBox checked icon="shopping-cart" iconUncheckedStyle="solid" />
  </div>
);

export const TemplateSizes = (args) => (
  <div style ={{display: 'flex', gap: '12px'}}>
    <CheckBox {...args} size="small" />
    <CheckBox {...args} size="medium" />
    <CheckBox {...args} size="large" />
  </div>
);

export const TemplateColors = (args) => (
  <div style ={{display: 'flex', gap: '12px'}}>
    <CheckBox {...args} color="primary" />
    <CheckBox {...args} color="secondary" />
    <CheckBox {...args} color="success" />
    <CheckBox {...args} color="warning" />
    <CheckBox {...args} color="danger" />
    <CheckBox {...args} color="info" />
  </div>
);

# CheckBox

<Canvas withSource="open">
  <Story
    name="Default"
    args={{
      checked: true,
    }}
  >
    {TemplateDefault.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Default" />

## Disabled state

<Canvas>
  <Story
    name="Disabled"
    args={{
      checked: true,
      disabled: true,
    }}
  >
    {TemplateDefault.bind({})}
  </Story>
</Canvas>

## Indeterminate state

<Canvas>
  <Story
    name="Indeterminate"
    args={{
      indeterminate: true,
    }}
  >
    {TemplateDefault.bind({})}
  </Story>
</Canvas>

## Icon checkmark

When `check={false}` icon style can be set to be `solid` or `regular` using `iconUncheckedStyle` property.

<Canvas withSource="open">
  <Story name="Icon">
    {TemplateIcon.bind({})}
  </Story>
</Canvas>

## Sizes

Defining `size` with `small`, `medium` and `large` values.

<Canvas>
  <Story
    name="Sizes"
    args={{
      checked: true,
    }}
  >
    {TemplateSizes.bind({})}
  </Story>
</Canvas>

Defining `color` 

<Canvas >
  <Story
    name="Colors"
    args={{
      size: "small",
      checked: true,
    }}
  >
    {TemplateColors.bind({})}
  </Story>
</Canvas>