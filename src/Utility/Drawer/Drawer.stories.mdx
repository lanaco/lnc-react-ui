import { Meta, Story, Canvas, Props } from "@storybook/addon-docs/blocks";
import { ArgsTable, Stories } from "@storybook/addon-docs";
import Drawer from "./index";
import { useState, useRef } from "react";
import Button from "../../General/Button";
import { Prism as SyntaxHighlighter } from "react-syntax-highlighter";
import { nord } from "react-syntax-highlighter/dist/esm/styles/prism";

<Meta title="Utility/Drawer" component={Drawer} />

# Drawer

<SyntaxHighlighter language="jsx" style={nord}>
  {'import { Drawer } from "@lanaco/lnc-react-ui";'}
</SyntaxHighlighter>

<Story name="Default" args={{}}>
  {(args) => {
    const drawerRef = useRef();
    const openDrawer = () => {
      drawerRef?.current?.open();
    };
    return (
      <>
        <Button onClick={openDrawer}>Open Drawer</Button>
        <Drawer ref={drawerRef} {...args}>
          This is Drawer
        </Drawer>
      </>
    );
  }}
</Story>

<SyntaxHighlighter language="jsx" style={nord}>
  {"(args) => {\n" +
    " const drawerRef = useRef();\n" +
    " const openDrawer = () => {\n" +
    "   drawerRef?.current?.open();\n" +
    " };\n" +
    " return (\n" +
    "   <>\n" +
    "     <Button onClick={openDrawer}>Open Drawer</Button>\n" +
    "     <Drawer ref={drawerRef} {...args}>\n" +
    "       This is Drawer\n" +
    "     </Drawer>\n" +
    "   </>\n" +
    " );\n" +
    "};"}
</SyntaxHighlighter>

<ArgsTable story="Default" />

## Direction

<Story name="Direction" args={{}}>
  {(args) => {
    const drawerRef = useRef();
    const [direction, setDirection] = useState("right");
    const openDrawer = (dir) => {
      setDirection(dir);
      drawerRef?.current?.open();
    };
    return (
      <>
        <Button onClick={() => openDrawer("right")}>Right</Button>
        &nbsp;&nbsp;
        <Button onClick={() => openDrawer("left")}>Left</Button>
        &nbsp;&nbsp;
        <Button onClick={() => openDrawer("top")}>Top</Button>
        &nbsp;&nbsp;
        <Button onClick={() => openDrawer("bottom")}>Bottom</Button>
        <Drawer ref={drawerRef} direction={direction} {...args}>
          This is Drawer
        </Drawer>
      </>
    );
  }}
</Story>

<SyntaxHighlighter language="jsx" style={nord}>
  {"(args) => {\n" +
    " const drawerRef = useRef();\n" +
    ' const [direction, setDirection] = useState("right");\n' +
    " const openDrawer = (dir) => {\n" +
    "   setDirection(dir);\n" +
    "   drawerRef?.current?.open();\n" +
    "   };\n" +
    " return (\n" +
    "   <>\n" +
    '     <Button onClick={() => openDrawer("right")}>Right</Button>\n' +
    "     &nbsp;&nbsp;\n" +
    '     <Button onClick={() => openDrawer("left")}>Left</Button>\n' +
    "     &nbsp;&nbsp;\n" +
    '     <Button onClick={() => openDrawer("top")}>Top</Button>\n' +
    "     &nbsp;&nbsp;\n" +
    '     <Button onClick={() => openDrawer("bottom")}>Bottom</Button>\n' +
    "     <Drawer ref={drawerRef} direction={direction} {...args}>\n" +
    "       This is Drawer\n" +
    "     </Drawer>\n" +
    "   </>\n" +
    " );\n" +
    "};"}
</SyntaxHighlighter>

## Colors

<Story name="Colors" args={{}}>
  {(args) => {
    const drawerRef = useRef();
    const [color, setColor] = useState("neutral");
    const openDrawer = (col) => {
      setColor(col);
      drawerRef?.current?.open();
    };
    return (
      <>
        <Button onClick={() => openDrawer("neutral")} color="neutral">
          Neutral
        </Button>
        &nbsp;&nbsp;
        <Button onClick={() => openDrawer("primary")} color="primary">
          Primary
        </Button>
        &nbsp;&nbsp;
        <Button onClick={() => openDrawer("secondary")} color="secondary">
          Secondary
        </Button>
        &nbsp;&nbsp;
        <Button onClick={() => openDrawer("success")} color="success">
          Success
        </Button>
        &nbsp;&nbsp;
        <Button onClick={() => openDrawer("danger")} color="danger">
          Danger
        </Button>
        &nbsp;&nbsp;
        <Button onClick={() => openDrawer("warning")} color="warning">
          Warning
        </Button>
        &nbsp;&nbsp;
        <Button onClick={() => openDrawer("information")} color="information">
          Information
        </Button>
        <Drawer ref={drawerRef} color={color} {...args}>
          This is Drawer
        </Drawer>
      </>
    );
  }}
</Story>

<SyntaxHighlighter language="jsx" style={nord}>
  {"(args) => {\n" +
    " const drawerRef = useRef();\n" +
    ' const [color, setColor] = useState("neutral");\n' +
    " const openDrawer = (col) => {\n" +
    "   setColor(col);\n" +
    "   drawerRef?.current?.open();\n" +
    " };\n" +
    " return (\n" +
    "   <>\n" +
    '     <Button onClick={() => openDrawer("neutral")} color="neutral">\n' +
    "       Neutral\n" +
    "     </Button>\n" +
    "     &nbsp;&nbsp;\n" +
    '     <Button onClick={() => openDrawer("primary")} color="primary">\n' +
    "       Primary\n" +
    "     </Button>\n" +
    "     &nbsp;&nbsp;\n" +
    '     <Button onClick={() => openDrawer("secondary")} color="secondary">\n' +
    "       Secondary\n" +
    "     </Button>\n" +
    "     &nbsp;&nbsp;\n" +
    '     <Button onClick={() => openDrawer("success")} color="success">\n' +
    "       Success\n" +
    "     </Button>\n" +
    "     &nbsp;&nbsp;\n" +
    '     <Button onClick={() => openDrawer("danger")} color="danger">\n' +
    "       Danger\n" +
    "     </Button>\n" +
    "     &nbsp;&nbsp;\n" +
    '     <Button onClick={() => openDrawer("warning")} color="warning">\n' +
    "       Warning\n" +
    "     </Button>\n" +
    "     &nbsp;&nbsp;\n" +
    '     <Button onClick={() => openDrawer("information")} color="information">\n' +
    "       Information\n" +
    "     </Button>\n" +
    "     <Drawer ref={drawerRef} color={color} {...args}>\n" +
    "       This is Drawer\n" +
    "     </Drawer>\n" +
    "   </>\n" +
    " );\n" +
    "};"}
</SyntaxHighlighter>

## Sizes

<Story name="Sizes" args={{}}>
  {(args) => {
    const drawerRef = useRef();
    const [size, setSize] = useState("small");
    const openDrawer = (s) => {
      setSize(s);
      drawerRef?.current?.open();
    };
    return (
      <>
        <Button onClick={() => openDrawer("small")}>Small</Button>
        &nbsp;&nbsp;
        <Button onClick={() => openDrawer("medium")}>Medium</Button>
        &nbsp;&nbsp;
        <Button onClick={() => openDrawer("large")}>Large</Button>
        <Drawer ref={drawerRef} size={size} {...args}>
          This is Drawer
        </Drawer>
      </>
    );
  }}
</Story>

<SyntaxHighlighter language="jsx" style={nord}>
  {"(args) => {\n" +
    " const drawerRef = useRef();\n" +
    ' const [size, setSize] = useState("small");\n' +
    " const openDrawer = (s) => {\n" +
    "   setSize(s);\n" +
    "   drawerRef?.current?.open();\n" +
    " };\n" +
    " return (\n" +
    "   <>\n" +
    '     <Button onClick={() => openDrawer("small")}>Small</Button>\n' +
    "     &nbsp;&nbsp;\n" +
    '     <Button onClick={() => openDrawer("medium")}>Medium</Button>\n' +
    "     &nbsp;&nbsp;\n" +
    '     <Button onClick={() => openDrawer("large")}>Large</Button>\n' +
    "     <Drawer ref={drawerRef} size={size} {...args}>\n" +
    "       This is Drawer\n" +
    "     </Drawer>\n" +
    "   </>\n" +
    " );\n" +
    "};"}
</SyntaxHighlighter>

#### This component is built on top of [react-modern-drawer](https://www.npmjs.com/package/react-modern-drawer). [See more](https://github.com/Farzin-Firoozi/react-modern-drawer#readme).
