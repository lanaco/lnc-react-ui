import React from "react";
import Popover from ".";

export default {
  title: "Utility/Popover",
  component: Popover
};

const Template = (args) => (
  <Popover />
);

export const Default = Template.bind({});
