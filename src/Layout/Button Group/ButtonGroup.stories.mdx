import { Meta, Story, Canvas, Props } from "@storybook/addon-docs/blocks";
import { ArgsTable, Stories } from "@storybook/addon-docs";
import IconButton from "../../General/IconButton/index";
import Button from "../../General/Button/index";
import ButtonGroup from ".";

<Meta title="Layout/Button Group" component={ButtonGroup} />

export const TemplateDefault = (args) => (
  <ButtonGroup {...args}>
    <Button text="Left" btnType="basic" />
    <Button text="Center" btnType="basic" />
    <Button text="Right" btnType="basic" />
  </ButtonGroup>
);

export const TemplateBorderRadius = (args) => (
  <div>
    <div style={{ display: "flex", gap: "16px", marginBottom: "16px" }}>
      <ButtonGroup {...args}>
        <Button text="Left" btnType="basic" />
        <Button text="Center" btnType="basic" />
        <Button text="Right" btnType="basic" />
      </ButtonGroup>
    </div>
    <div style={{ display: "flex", gap: "16px" }}>
      <ButtonGroup {...args} borderRadius="curved">
        <Button text="Left" btnType="basic" />
        <Button text="Center" btnType="basic" />
        <Button text="Right" btnType="basic" />
      </ButtonGroup>
    </div>
  </div>
);

export const TemplateDisabled = (args) => (
  <ButtonGroup {...args}>
    <Button disabled={true} text="Left" btnType="basic" />
    <Button disabled={true} text="Center" btnType="basic" />
    <Button disabled={true} text="Right" btnType="basic" />
  </ButtonGroup>
);

export const TemplateCombinations = (args) => (
  <div>
    <div style={{ display: "flex", gap: "16px", marginBottom: "16px" }}>
      <ButtonGroup {...args}>
        <Button text="Save" leadingIcon="save" btnType="tinted" />
        <Button text="or" type="basic" disabled />
        <Button
          text="Discard"
          leadingIcon="times"
          btnType="tinted"
          color="danger"
        />
      </ButtonGroup>
    </div>
    <div style={{ display: "flex", gap: "16px", marginBottom: "16px" }}>
      <ButtonGroup {...args} borderRadius="curved">
        <IconButton icon="align-left" btnType="basic" />
        <IconButton icon="align-center" btnType="tinted" color="secondary" />
        <IconButton icon="align-right" btnType="basic" />
      </ButtonGroup>
    </div>
    <div style={{ display: "flex", gap: "16px", marginBottom: "16px" }}>
      <ButtonGroup>
        <IconButton
          disabled={true}
          btnType="basic"
          icon="angle-double-left"
          color="neutral"
        />
        <IconButton
          disabled={true}
          btnType="basic"
          icon="angle-left"
          color="neutral"
        />
        <Button btnType="tinted" text="1" color="primary" />
        <Button btnType="basic" text="2" color="neutral" />
        <Button btnType="basic" text="3" color="neutral" />
        <IconButton btnType="basic" icon="angle-right" color="neutral" />
        <IconButton btnType="basic" icon="angle-double-right" color="neutral" />
      </ButtonGroup>
    </div>
    <div style={{ display: "flex", gap: "16px", marginBottom: "16px" }}>
      <ButtonGroup {...args}>
        <Button
          text="Apply"
          leadingIcon="check-circle"
          btnType="tinted"
          color="secondary"
        />
        <Button
          text="Cancel"
          leadingIcon="times"
          btnType="basic"
          color="neutral"
        />
      </ButtonGroup>
    </div>
    <div style={{ display: "flex", gap: "16px" }}>
      <ButtonGroup {...args} borderRadius="curved">
        <Button text="Year" btnType="filled" color="primary" />
        <Button text="Month" btnType="tinted" color="primary" />
        <Button text="Day" btnType="basic" color="primary" />
      </ButtonGroup>
    </div>
  </div>
);

# Button Group

```jsx
import { ButtonGroup } from "@lanaco/lnc-react-ui";
```

<Canvas>
  <Story name="Default">{TemplateDefault.bind({})}</Story>
</Canvas>

<ArgsTable story="Default" />

## Border Radius

`borderRadius:` `regular` / `curved`

<Canvas>
  <Story name="Border Radius">{TemplateBorderRadius.bind({})}</Story>
</Canvas>

## Disabled State

<Canvas>
  <Story name="Disabled State">{TemplateDisabled.bind({})}</Story>
</Canvas>

## Combinations

<Canvas>
  <Story name="Combinations">{TemplateCombinations.bind({})}</Story>
</Canvas>
