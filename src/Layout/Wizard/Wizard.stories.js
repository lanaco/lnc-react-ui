import React from "react";
import Wizard from ".";

export default {
  title: "Layout/Wizard",
  component: Wizard
};

const Template = (args) => (
  <Wizard />
);

export const Default = Template.bind({});
